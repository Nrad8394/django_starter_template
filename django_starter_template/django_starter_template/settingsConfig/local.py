"""
Local Settings - Machine-Specific Overrides
============================================

This file is optional and allows for machine-specific settings overrides.

To use, define any local-only settings here. This file is typically
NOT committed to version control and is ignored by .gitignore.

Example (uncomment to use):
  # Override database for local development
  # DATABASES['default']['NAME'] = BASE_DIR / 'local_db.sqlite3'

  # Override cache location
  # CACHES['default']['LOCATION'] = 'redis://localhost:6379/0'
"""

# No settings here - all configuration is in settingsConfig modules

